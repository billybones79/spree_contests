$(".contest").html("<%=j render :partial => 'spree/shared/participation_confirm', :locals => { :participation => @participation } %>");



$("#share").click(function(e){
    var urlpost = <%= contest_url(@participation.contest) %>
    e.preventDefault();
    FB.ui({
        method: 'share',
        href: urlpost
    }, function(response){

    });
});
